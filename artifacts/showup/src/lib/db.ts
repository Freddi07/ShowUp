// Client-side stub — database access is server-only.
// All DB operations go through the API server (artifacts/api-server).
// This file exists only so server-side imports don't break the Vite build.

export const prisma = null;
